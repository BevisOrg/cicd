services:
  app:
    image: ${IMAGE}
    container_name: ${CONTAINER}
    ports:
      - "3000:80"
    environment:
      PROFILE: "prod"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      
